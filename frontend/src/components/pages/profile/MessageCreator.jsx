import * as React from "react";
import {Container} from "semantic-ui-react";

export const MessageCreator = props => {

    const {receiver} = props;

    return <Container>
        TODO: INPUT FOR SENDING MESSAGE
    </Container>
};