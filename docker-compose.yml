version: '3.3'

services:

  redis:
    image: "redis:latest"

  registry:
    build:
      context: ./registry
      dockerfile: Dockerfile

  gateway:
    build:
      context: ./gateway
      dockerfile: Dockerfile
    ports:
      - "80:8080"
    depends_on:
      - registry
      - redis

  postgres_authentication:
    image: "postgres:10"

  postgres_movies:
    image: "postgres:10"

  authentication:
    build:
      context: ./authentication
      dockerfile: Dockerfile
    depends_on:
      - postgres_authentication
      - gateway


  movies_0:
    build:
      context: ./movies
      dockerfile: Dockerfile
    environment:
      - MOVIES_LB_ID=A
    depends_on:
      - postgres_movies
      - gateway

  movies_1:
    build:
      context: ./movies
      dockerfile: Dockerfile
    environment:
      - MOVIES_LB_ID=B
    depends_on:
      - postgres_movies
      - gateway

  movies_2:
    build:
      context: ./movies
      dockerfile: Dockerfile
    environment:
      - MOVIES_LB_ID=C
    depends_on:
      - postgres_movies
      - gateway